/**
 * Créer:
 * - une variable contenant du texte appelé var1
 * - une variable contenant un nombre appelé var2
 * - un tableau vide appelé emptyArr
 * - un tableau appelé filledArr contenant dans cet ordre var1 et var2
 */
const var1 = "Hello";
const var2 = 4;
const emptyArr = [];
const filledArr = [var1, var2];

/**
 * ne pas effacer cette partie
 */
module.exports = {
  var1,
  var2,
  emptyArr,
  filledArr,
};
